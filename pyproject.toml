[project]
name = "easyinput"
version = "0.1.0"
description = "A Universal Text Input Simulator"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pynput>=1.8.1",
]

[dependency-groups]
dev = [
    "nuitka>=4.0.5",
]
